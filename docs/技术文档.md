# 技术文档

## 📋 目录

- [技术栈](#技术栈)
- [开发环境](#开发环境)
- [项目结构](#项目结构)
- [核心模块](#核心模块)
- [API接口](#api接口)
- [数据库设计](#数据库设计)
- [前端技术](#前端技术)
- [第三方集成](#第三方集成)

---

## 技术栈

### 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | 后端框架 |
| MyBatis-Plus | 3.5.3 | ORM框架 |
| MySQL | 8.0 | 关系型数据库 |
| JWT | - | 身份认证 |
| OkHttp3 | 4.9.3 | HTTP客户端（OCR调用） |
| Apache POI | 5.2.3 | Excel导出 |
| iText | 7.2.5 | PDF导出 |
| Lombok | 1.18.24 | 简化代码 |
| Hutool | 5.8.16 | 工具类库 |
| Spring Boot Mail | 2.7.18 | 邮件发送 |

### 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| HTML5 | - | 页面结构 |
| CSS3 | - | 样式设计 |
| JavaScript (ES6+) | - | 交互逻辑 |
| Chart.js | 3.9.1 | 数据可视化 |

### 部署技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Docker | 20.10+ | 容器化 |
| Docker Compose | 1.29+ | 容器编排 |
| Nginx | 1.24-alpine | 反向代理 |

---

## 开发环境

### 必需软件

- **JDK**: 1.8 或更高版本
- **Maven**: 3.6+ 
- **MySQL**: 8.0（本地开发）
- **Docker**: 20.10+（部署）
- **Git**: 2.30+（版本控制）

### IDE推荐

- **IntelliJ IDEA**: 2021.3+（推荐）
- **Eclipse**: 2021-12+
- **VS Code**: 1.70+（前端开发）

### 开发工具

- **Postman**: API测试
- **Navicat/DBeaver**: 数据库管理
- **Chrome DevTools**: 前端调试

---

## 项目结构

```
health-cert-system/
├── docker/                      # Docker配置
│   └── nginx/
│       └── nginx.conf          # Nginx配置
├── health-cert-backend/        # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/microport/healthcert/
│   │   │   │       ├── common/          # 通用类
│   │   │   │       ├── config/          # 配置类
│   │   │   │       ├── controller/      # 控制器
│   │   │   │       ├── dto/             # 数据传输对象
│   │   │   │       ├── entity/          # 实体类
│   │   │   │       ├── exception/       # 异常处理
│   │   │   │       ├── interceptor/     # 拦截器
│   │   │   │       ├── mapper/          # MyBatis映射器
│   │   │   │       ├── service/         # 服务接口
│   │   │   │       │   └── impl/        # 服务实现
│   │   │   │       ├── task/            # 定时任务
│   │   │   │       ├── util/            # 工具类
│   │   │   │       └── vo/              # 视图对象
│   │   │   └── resources/
│   │   │       ├── application.yml      # 应用配置
│   │   │       ├── mapper/              # MyBatis XML映射
│   │   │       ├── sql/                 # SQL脚本
│   │   │       └── static/               # 静态资源（前端）
│   │   └── test/                        # 测试代码
│   ├── Dockerfile                       # Docker构建文件
│   └── pom.xml                          # Maven依赖配置
├── docker-compose.yml                   # Docker Compose配置
└── README.md                            # 项目说明
```

---

## 核心模块

### 1. 认证授权模块

**位置**: `com.microport.healthcert.controller.AuthController`

**功能**:
- 用户登录（管理员/员工）
- JWT Token生成和验证
- 密码加密（BCrypt）

**关键类**:
- `AuthController`: 登录接口
- `AuthService`: 认证服务
- `JwtUtil`: JWT工具类
- `JwtInterceptor`: JWT拦截器

### 2. 健康证管理模块

**位置**: `com.microport.healthcert.controller.HealthCertController`

**功能**:
- 健康证上传
- OCR自动识别
- 健康证查询
- 历史记录查看

**关键类**:
- `HealthCertController`: 员工端接口
- `AdminHealthCertController`: 管理员端接口
- `HealthCertService`: 健康证服务
- `OcrService`: OCR识别服务

### 3. 审核管理模块

**位置**: `com.microport.healthcert.controller.AdminHealthCertController`

**功能**:
- 健康证审核（通过/拒绝）
- 审核记录查询
- 审核锁定机制

**关键类**:
- `AdminHealthCertService`: 审核服务
- `AuditLock`: 审核锁定实体

### 4. 员工管理模块

**位置**: `com.microport.healthcert.controller.EmployeeController`

**功能**:
- 员工信息查询
- 员工信息编辑
- 分页查询

**关键类**:
- `EmployeeController`: 员工接口
- `EmployeeService`: 员工服务
- `EmployeeMapper`: 员工数据访问

### 5. 部门管理模块

**位置**: `com.microport.healthcert.controller.DepartmentController`

**功能**:
- 部门树状结构查询
- 部门信息管理
- 部门启用/禁用

**关键类**:
- `DepartmentController`: 部门接口
- `DepartmentService`: 部门服务
- `DepartmentTreeVO`: 部门树视图对象

### 6. 数据看板模块

**位置**: `com.microport.healthcert.controller.DashboardController`

**功能**:
- 统计数据查询
- 图表数据生成
- 下钻功能（员工列表）

**关键类**:
- `DashboardController`: 看板接口
- `DashboardService`: 看板服务
- `DashboardOverviewVO`: 概览数据对象

### 7. 同步服务模块

**位置**: `com.microport.healthcert.service.SyncService`

**功能**:
- 员工数据同步（从远程HR系统）
- 部门数据同步
- 定时自动同步

**关键类**:
- `SyncService`: 同步服务
- `EmployeeSyncTask`: 员工同步任务
- `RemoteDataSourceConfig`: 远程数据源配置

### 8. 提醒服务模块

**位置**: `com.microport.healthcert.service.ReminderService`

**功能**:
- 健康证到期提醒（邮件/钉钉）
- 定时任务触发
- 提醒记录

**关键类**:
- `ReminderService`: 提醒服务
- `ReminderTask`: 提醒任务
- `EmailService`: 邮件服务
- `DingTalkService`: 钉钉服务

### 9. 导出服务模块

**位置**: `com.microport.healthcert.service.ExportService`

**功能**:
- Excel格式导出
- PDF格式导出
- 数据筛选导出

**关键类**:
- `ExportService`: 导出服务
- `ExportRequestDTO`: 导出请求对象

### 10. 系统配置模块

**位置**: `com.microport.healthcert.controller.ConfigController`

**功能**:
- 邮件配置管理
- 钉钉配置管理
- 提醒规则配置

**关键类**:
- `ConfigController`: 配置接口
- `ConfigService`: 配置服务
- `SystemConfig`: 系统配置实体

---

## API接口

### 认证接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录 | 公开 |
| GET | `/api/auth/health` | 健康检查 | 公开 |

### 健康证接口（员工端）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/health-cert/submit` | 提交健康证 | 员工 |
| GET | `/api/health-cert/my-list` | 我的健康证列表 | 员工 |
| GET | `/api/health-cert/{id}` | 查看健康证详情 | 员工 |

### 健康证接口（管理员端）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/admin/health-cert/list` | 健康证列表 | 管理员 |
| GET | `/api/admin/health-cert/{id}` | 健康证详情 | 管理员 |
| POST | `/api/admin/health-cert/approve` | 审核通过 | 管理员 |
| POST | `/api/admin/health-cert/reject` | 审核拒绝 | 管理员 |
| POST | `/api/admin/health-cert/export` | 导出健康证 | 管理员 |

### 员工管理接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/admin/employees` | 员工列表（分页） | 管理员 |
| PUT | `/api/admin/employees/{id}` | 更新员工信息 | 管理员 |

### 部门管理接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/admin/departments/tree` | 部门树结构 | 管理员 |
| PUT | `/api/admin/departments/{id}/status` | 启用/禁用部门 | 管理员 |

### 数据看板接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/admin/dashboard/overview` | 概览统计 | 管理员 |
| GET | `/api/admin/dashboard/charts` | 图表数据 | 管理员 |
| GET | `/api/admin/dashboard/employee-list` | 下钻员工列表 | 管理员 |

### 系统配置接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/admin/config/email` | 获取邮件配置 | 管理员 |
| PUT | `/api/admin/config/email` | 更新邮件配置 | 管理员 |
| POST | `/api/admin/config/email/test` | 测试邮件发送 | 管理员 |
| GET | `/api/admin/config/dingtalk` | 获取钉钉配置 | 管理员 |
| PUT | `/api/admin/config/dingtalk` | 更新钉钉配置 | 管理员 |
| GET | `/api/admin/config/reminder` | 获取提醒配置 | 管理员 |
| PUT | `/api/admin/config/reminder` | 更新提醒配置 | 管理员 |
| POST | `/api/admin/config/sync` | 手动触发同步 | 管理员 |

---

## 数据库设计

### 核心表结构

#### 1. employees（员工表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| sf_user_id | VARCHAR(50) | 域账号 |
| mp_number | VARCHAR(50) | 工号 |
| name | VARCHAR(100) | 姓名 |
| depart_name_cn | VARCHAR(200) | 部门名称 |
| email | VARCHAR(200) | 邮箱 |
| mobile | VARCHAR(20) | 手机号 |
| dingtalk_userid | VARCHAR(100) | 钉钉用户ID |
| is_active | TINYINT | 在职状态（0离职/1在职） |
| is_frontline_worker | TINYINT | 是否一线员工 |

#### 2. health_certificates（健康证表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| cert_number | VARCHAR(100) | 健康证编号（唯一） |
| employee_id | BIGINT | 员工ID |
| employee_name | VARCHAR(100) | 员工姓名 |
| cert_number | VARCHAR(100) | 健康证编号 |
| issue_date | DATE | 发证日期 |
| expiry_date | DATE | 有效期至 |
| image_path | VARCHAR(500) | 图片路径 |
| status | VARCHAR(20) | 状态（draft/pending/approved/rejected/expired） |
| is_current | TINYINT | 是否当前有效（0否/1是） |
| version | INT | 版本号 |

#### 3. admins（管理员表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| username | VARCHAR(50) | 用户名（唯一） |
| password | VARCHAR(255) | 密码（BCrypt加密） |
| real_name | VARCHAR(100) | 真实姓名 |
| email | VARCHAR(200) | 邮箱 |
| role | VARCHAR(50) | 角色 |

#### 4. departments（部门表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| dept_code | VARCHAR(50) | 部门编码 |
| dept_name_cn | VARCHAR(200) | 部门名称 |
| parent_id | BIGINT | 父部门ID |
| supervisor_sf_user_id | VARCHAR(50) | 主管域账号 |
| is_enabled | TINYINT | 是否启用 |

#### 5. system_configs（系统配置表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| config_key | VARCHAR(100) | 配置键（唯一） |
| config_value | TEXT | 配置值（JSON格式） |
| description | VARCHAR(500) | 描述 |

### 数据库关系

```
employees (1) ──< (N) health_certificates
departments (1) ──< (N) employees
departments (1) ──< (N) departments (parent_id)
```

---

## 前端技术

### 页面结构

- **登录页**: `login.html`
- **员工首页**: `employee-home.html`
- **健康证上传**: `upload-certificate.html`
- **管理员工作台**: `admin-dashboard.html`
- **健康证管理**: `cert-audit.html`
- **员工管理**: `employee-management.html`
- **部门管理**: `department-management.html`
- **操作日志**: `operation-logs.html`
- **系统配置**: `system-config.html`

### 公共JS文件

- **api.js**: API请求封装，自动添加Token，统一错误处理
- **common.js**: 通用工具函数（加载提示、消息提示、日期格式化等）

### 前端特性

- **响应式设计**: 支持PC和移动端
- **数据可视化**: Chart.js饼图和柱状图
- **交互体验**: 模态框、下拉菜单、分页等
- **状态管理**: LocalStorage存储Token和用户信息

---

## 第三方集成

### OCR服务（PaddleOCR）

**配置位置**: `application.yml`

```yaml
ocr:
  service:
    url: http://10.11.100.238:8081/ocr
    timeout: 30000
```

**调用方式**: POST请求，JSON格式，Base64编码图片

**返回格式**: JSON，包含识别文本和位置信息

### 钉钉集成

**功能**: 健康证到期提醒

**配置**: 系统配置页面设置corp_id和app_secret

**API**: 钉钉开放平台工作通知API

### 邮件服务

**功能**: 健康证到期提醒

**配置**: 系统配置页面设置SMTP参数

**支持**: SMTP协议，支持SSL/TLS

---

## 开发规范

### 代码规范

- **命名规范**: 
  - 类名：大驼峰（PascalCase）
  - 方法名/变量名：小驼峰（camelCase）
  - 常量：大写下划线（UPPER_SNAKE_CASE）
  - 包名：小写，点分隔

- **注释规范**:
  - 类和方法使用JSDoc注释
  - 复杂逻辑添加行内注释
  - 中文注释

### Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
test: 测试相关
chore: 构建/工具相关
```

---

## 测试

### 单元测试

- 使用JUnit 5
- Mockito进行Mock
- 测试覆盖率目标：60%+

### 集成测试

- Spring Boot Test
- 数据库测试使用H2内存数据库

### API测试

- Postman集合
- 自动化测试脚本

---

**最后更新**: 2024年12月

